import type { DashboardLessonStatus } from '../../../shared/dashboard-contracts';

export function LessonMap({ lessons, onOpenLessons }: { lessons: DashboardLessonStatus[]; onOpenLessons: () => void }) {
  return <section className="parent-card lesson-status-card" aria-labelledby="lesson-map-title"><div className="parent-card-heading"><div><p className="eyebrow">BẢN ĐỒ 6 VÙNG</p><h2 id="lesson-map-title">Trạng thái 29 bài</h2></div><button className="text-button" type="button" onClick={onOpenLessons}>Xem bài <span>→</span></button></div><div className="parent-scroll-region" role="region" tabIndex={0} aria-label="Danh sách trạng thái 29 bài" data-parent-scroll-region="lessons">{lessons.map((lesson, index) => <div className="parent-lesson-row" key={lesson.lessonId}><span className={`parent-lesson-badge is-${index % 2 ? 'coral' : 'sun'}`}>{String(index + 1).padStart(2, '0')}</span><div><strong>{lesson.title}</strong><small>{lesson.completedMissions}/{lesson.totalMissions} nhiệm vụ · {lesson.topic}</small></div><span className={`ready-badge dashboard-lesson-status is-${lesson.status}`}>{lesson.status === 'complete' ? 'Đã nhận dấu' : lesson.status === 'in-progress' ? 'Đang học' : lesson.status === 'open' ? 'Đang mở' : 'Chưa mở'}</span></div>)}</div></section>;
}
